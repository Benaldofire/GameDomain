
    <!--The value attribute specifies the value to be sent to a server when a form is submitted
        If the value attribute is not specified, the content will be passed as a value instead.

        Note: the server sends back the parameters selected for the filter and it's the repopulated into the filter again
        (for the label) The value of the "for" attribute must be a single "id" (for the input)
    -->
    <div class="filter-title"> Filter By: </div>
    <div class="dropdown">
        <div class="dropdown-name"> <span>Genres</span> <i class="fa fa-caret-down"></i></div>
        <div class="dropdown-content">
            <div>
                <div> 
                <input type="checkbox" name="action" id="action" <%if(action){%>checked<%}%>> 
                <label for="action" class="labels">Action</label>
                </div>
                
                <div>
                <input type="checkbox" name="rpg" id="rpg" <%if(rpg){%>checked<%}%>>
                <label for="rpg" class="labels">RPG</label>
                </div>
                
                <div>
                <input type="checkbox" name="shooter" id="shooter" <%if(shooter){%>checked<%}%>>
                <label for="shooter" class="labels">Shooter</label>
                </div>
                
                <div>
                <input type="checkbox" name="battleroyale" id= "battleroyale" <%if(battleroyale){%>checked<%}%>>
                <label for="battleroyale" class="labels">Battle Royale</label>
                </div>
            </div>
        </div>
    </div>

    <div>
        <label class="filter-title"> Sort </label>
        <select name = "sort" class="selection">
            <option class="labels" label="A to Z" value="asc" <%if(sort === "asc"){%>selected<%}%>></option>
            <option class="labels" label ="Z to A" value= "desc"<%if(sort === "desc"){%>selected<%}%>></option>
        </select>
    
        <select name = "sortby" class = "selection">
            <option class="labels" label="Name" value="name" <%if(sortby === "name"){%>selected<%}%>></option>
            <option class="labels" label="Release Date" value="release_Date" <%if(sortby === "release_Date"){%>selected<%}%>></option>
            <option class="labels" label ="Rating" value= "rating"<%if(sortby === "rating"){%>selected<%}%>></option>
        </select>
    </div>







<!-- want to send a post request once we click search-->